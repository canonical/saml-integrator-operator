# Copyright 2024 Canonical Ltd.
# See LICENSE file for licensing details.

# Learn more about charmcraft.yaml configuration at:
# https://juju.is/docs/sdk/charmcraft-config
type: "charm"
bases:
  - build-on:
      - name: "ubuntu"
        channel: "22.04"
    run-on:
      - name: "ubuntu"
        channel: "22.04"
parts:
  charm:
    build-packages:
      - libxml2-dev
      - libxslt-dev
      - python3-dev
      - libffi-dev
      - libssl-dev
      - pkg-config
    build-snaps:
      - rustup
    charm-requirements:
      - requirements-charmcraft.txt
    override-build: |
      rustup default stable
      craftctl default
      # charmcraft filters all environment variables, build static linked variant of lxml here
      export CFLAGS="-O3 -g1 -pipe -fPIC -flto"
      export LDFLAGS="-fPIC -flto"
      export STATIC_DEPS="true"
      $CRAFT_PART_BUILD/staging-venv/bin/pip install --no-binary :all: -r requirements-static.txt
