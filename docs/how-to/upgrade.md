# How to upgrade

Upgrade the `saml-integrator` charm with the `refresh` command:

```bash
juju refresh saml-integrator
```

Check that the `saml-integrator` charm is active and running:

```bash
juju status
```
